@model IEnumerable<Virtual_School_Register.Models.ConductingLesson>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.Content)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConductingLessonId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConductingLessonId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConductingLessonId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
